#ifndef UTILS_H
#define UTILS_H

#include <string.h>
#include <stdlib.h>

char* generate_token(char*, char*) ;
void absolute_strcpy(char*, char*, char*) ;
char* extract_fname(char*) ;
char* remove_ext(char*) ;

#endif // UTILS_H
